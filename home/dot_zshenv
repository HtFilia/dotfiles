# Keep non-interactive SSH commands able to find user-installed tools.
# No subprocesses, terminal output or interactive plugin setup here.
typeset -U path PATH
path=("$HOME/.local/bin" "$HOME/.cargo/bin" "$HOME/go/bin" $path)
export PATH
